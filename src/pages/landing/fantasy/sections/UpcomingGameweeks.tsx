import { Link } from 'react-router-dom'; import { FiChevronRight } from 'react-icons/fi'; import type { FantasyCompetition } from '../../../../services/fantasyService'; import './UpcomingGameweeks.css';
export default function UpcomingGameweeks({competitions}:{competitions:FantasyCompetition[]}){const rows=competitions.filter(x=>x.current_gameweek);return <section className="fantasy-panel upcoming-gameweeks"><div className="fantasy-panel-heading"><h2>Current Gameweeks</h2><Link to="/fan/fantasy" className="fantasy-view-link">View Fantasy</Link></div><div className="gameweek-list">{rows.map(row=><div className="gameweek-row" key={row.id}><div className="gameweek-details"><span className="gameweek-league">{row.name}</span><span className="gameweek-number">{row.current_gameweek?.name}</span></div><div className="gameweek-countdown"><span className="gameweek-countdown-label">Deadline</span><span className="gameweek-countdown-value">{row.current_gameweek?.deadline_at?new Date(row.current_gameweek.deadline_at).toLocaleString():'Not scheduled'}</span></div><FiChevronRight className="gameweek-chevron"/></div>)}</div>{!rows.length&&<p>No current gameweeks.</p>}</section>}
