import { Link } from 'react-router-dom'; import { FiArrowRight,FiCalendar } from 'react-icons/fi'; import type { FantasyCompetition } from '../../../../services/fantasyService'; import './FeaturedLeagues.css';
export default function FeaturedLeagues({competitions}:{competitions:FantasyCompetition[]}){return <section className="fantasy-panel featured-leagues"><div className="fantasy-panel-heading"><h2>Featured Fantasy Competitions</h2><Link to="/fantasy" className="fantasy-view-link">View all competitions</Link></div><div className="league-cards">{competitions.slice(0,3).map(row=><article className={`league-card league-${row.sport}`} key={row.id}><div className="league-card-tint"/><div className="league-card-content"><h3 className="league-card-title">{row.name}<br/><span>{row.sport} fantasy</span></h3><div className="league-card-meta"><span><FiCalendar/> {row.current_gameweek?.name??'Schedule pending'}</span><span>Registration {row.registration_state.toLowerCase()}</span></div><Link to={`/fantasy/${row.id}`} className="league-card-join">View competition <FiArrowRight/></Link></div></article>)}</div>{!competitions.length&&<p>No Fantasy competitions are currently published.</p>}</section>}
